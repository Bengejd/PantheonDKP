name: Lint
on: [push]

jobs:
  lint:
    name: Lint
    runs-on: ubuntu-latest
    steps:
      - name: WoW Lua Lint
        uses: davidcraig/action-wow-lint@v1.0.1
        with:
          path-to-files: '../../src/init.lua'

